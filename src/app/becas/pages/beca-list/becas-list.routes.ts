import { Routes } from "@angular/router";
import { BecaListComponent } from "./beca-list.component";

export const BECA_LIST_ROUTES: Routes = [
    {
        path: '',
        component: BecaListComponent
    }
]